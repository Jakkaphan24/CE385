## ข้อ 1: 
เน้นให้ฟังก์ชันทำหน้าที่แค่ "เรื่องเดียว" แล้วส่งค่ากลับออกมา (return) โดยห้ามพิมพ์ผลลัพธ์ข้างในฟังก์ชันเด็ดขาด และยังสอนการตั้ง "ค่าเริ่มต้น" ไว้เผื่อกรณีที่ไม่ได้ส่งค่าอะไรเข้ามาให้ฟังก์ชันด้วย

## ข้อ 2: 
เน้นการดึงข้อมูลหรือคัดกรองข้อมูลด้วยคำสั่งอย่าง find, filter, และ some สิ่งสำคัญนี้คือห้ามเปลี่ยนแปลงข้อมูลต้นฉบับ และสอนดึงข้อมูลแบบ "กันโปรแกรมพัง" เวลาเจอตัวแปรที่ไม่มีค่า ด้วยการใช้เครื่องหมาย ?. และ ?? ดักไว้

## ข้อ 3: 
สั่งห้ามใช้ลูป for หรือ while  โดยให้เปลี่ยนมาใช้การเขียนคำสั่งต่อกันยาวๆ รวดเดียวจบผ่าน .map() (แปลงหน้าตาข้อมูล), .filter() (คัดเฉพาะข้อมูลที่ตรงเงื่อนไข), และ .reduce() (ยุบรวมข้อมูลให้เหลือค่าเดียว) และต้องป้องกันโปรแกรม Error เวลาเจอตัวแปรว่างเปล่า ด้วยการใส่ "ค่าเริ่มต้น" ให้ .reduce() ทุกครั้ง